[DEPENDENCIES]
# Keep out of the browser package. Everything here still builds, tests and
# runs natively; this only controls what a visitor downloads. Race-mode ghost
# genomes ship from src/blueball/assets/race_ghosts/, not genomes/.
ignoreDirs = ["/.venv", "/.claude", "/.worktrees", "/.superpowers", "/.pytest_cache", "/genomes", "/probes", "/tests", "/docs", "/tools"]
ignoreFiles = []
